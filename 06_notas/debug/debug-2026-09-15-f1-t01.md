# Debug Summary — F1-T01 — 2026-09-15

**Task e problema:** F1-T01 — a prévia do projeto Skip ficou em branco após a versão 0.0.4.

**Reprodução:** abrir `https://novo-projeto-f88ac--preview.goskip.app` na versão 0.0.4; o DOM continha apenas o badge "Criado com o Skip" e não renderizava o login. A migração estava aplicada, a pipeline QA passava e não havia erro de hook nos logs.

**Causa raiz:** um bloco de interface destinado ao Dashboard foi inserido dentro do componente `Login`. O bloco referenciava `proofOccurrence`, `user` e `runProof`, que não existem nesse escopo, causando `ReferenceError` durante a montagem do `Index` e impedindo o React de renderizar a página.

**Correção:** mover o bloco de provas para `Dashboard`, onde os estados e funções existem; corrigir também a referência `Texarea` para `Textarea`. Nenhuma regra de negócio, migração ou integração fora da F1-T01 foi alterada na correção.

**Verificação automática:** Skip v0.0.5 — setup, análise estática, build, integrações e teste passaram; migrações `0001_hv_access_control` e `0002_hv_harden_user_access` estão `applied`; prévia reaberta e login visível; tentativa com credencial inexistente permaneceu no login e gerou somente erro sanitizado de autenticação.

**Gate atual:** aguardando teste humano.

**Limitações:** ainda não foram criadas contas de teste nem executado o fluxo completo Operador → Admin → Conferente, pois isso requer credenciais sintéticas fornecidas/operadas pelo champion no teste humano. Nenhum dado real, IA, scraping ou publicação foi usado.
